version: "3.7"
services:
  web:
    image: wpdocker:v3
    env_file:
      - script/.env
    #environment:
    #  - FTP_PASS=/run/secrets/FTP_PASS
    #  - FTP_USER=/run/secrets/FTP_USER
    #  - MYSQL_ROOT_PWD=/run/secrets/MYSQL_ROOT_PWD
    #  - MYSQL_USER=/run/secrets/MYSQL_USER
    #  - MYSQL_USER_PWD=/run/secrets/MYSQL_USER_PWD
    #  - MYSQL_USER_DB=/run/secrets/MYSQL_USER_DB
    #  - SSH_PASS=/run/secrets/SSH_PASS
    ports:
    - "3306:3306"
    - "80:80"
    - "443:443"
    volumes:
    - home:/home
    - db:/var/lib/mysql
    dns:
    - 8.8.8.8
    - 8.8.4.4

#secrets:
#  FTP_PASS:
#    external: true
#  FTP_USER:
#    external: true
#  MYSQL_ROOT_PWD:
#    external: true
#  MYSQL_USER:
#    external: true
#  MYSQL_USER_PWD:
#    external: true
#  MYSQL_USER_DB:
#    external: true
#  SSH_PASS:
#    external: true
volumes:
  home:
  db:
#echo "" | docker secret create MYSQL_ROOT_PWD -
#echo "alperensah" | docker secret create MYSQL_USER -
#echo "" | docker secret create MYSQL_USER_PWD -
#echo "alperendb" | docker secret create MYSQL_USER_DB -
#echo "" | docker secret create FTP_PASS -
#echo "alperensah" | docker secret create FTP_USER -
#echo "" | docker secret create SSH_PASS -
